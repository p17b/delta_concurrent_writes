[project]
name = "delta-concurrent-writes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "deltalake>=1.4.2",
    "pandas>=3.0.0",
    "pyarrow>=23.0.1",
]
